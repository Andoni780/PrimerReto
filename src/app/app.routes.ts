import { Routes } from '@angular/router';
import { LiscenseListerComponent } from './liscense-lister/liscense-lister.component';

export const routes: Routes = [
    {
        path: '',
        component: LiscenseListerComponent,
    }
];
